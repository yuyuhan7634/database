完善分房逻辑：队列中多申请者可能争夺同一房号，高并发下可能出错，需要分布式锁或事务保护。

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260605154501327.png)

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260605154526642.png)

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260605154556762.png)

在分房审批，调房逻辑，和月底分房逻辑中增加了行级排他锁，在 SQL 末尾加上 `FOR UPDATE`。

在退房逻辑中加了一个校验 `applyType == 3`。如果有人传了一个分房申请单的单号进来，系统依然会往下走退房流程，这会导致严重的数据错乱。

在分房审批，调房和月底自动分房部分抽取了分房的逻辑做成一个函数

之后加了一个生成住房统计表的功能;

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260605154619578.png)

完善`AdminController.java`提供了住房情况，分数阈值等的查询接口

在`@Transactional(rollbackFor = Exception.class)` 的方法里，只要代码抛出了 `RuntimeException`，Spring 的底层事务就会立刻触发全局回滚命令。数据库还没来得及把`insert` 执行就被撤销，所以直接改成分数不达标就直接抛出异常

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260605154643543.png)

之后根据这个顺序安了claude:git,node.js,claude code,vscode,插件，vscode clause,deepseekapi,配ccswitch,

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260605154708110.png)

之后让ai大概给我完善了一下后端代码，进行了以下修改：

 抽取公共分配方法 `allocateHouseToApplicant`：将分房逻辑（更新申请单→更新房产→创建住户→生成账单）抽取为公共方法，`approveHousing` 与 `allocatePendingApplications` 共同调用，消除重复代码

新增 AllocationResult 记录类，封装分配结果，统一生成分配单文本

`approveHousing` 增加队列头部校验：只允许审批当前队列最前面的申请，防止绕过排队顺序

`deleteBillsByNameAndHouse` — 改为按 `owner_name` + `house_no` 联合删除，防止删除同名住户的账单

`approveTransfer` 增加原住房面积与数据库一致性校验

`submitApplication` 中新增：分房申请前检查申请人是否已有住房

新增 `scheduledMonthlyBillGeneration`，每月 1 号 08:00 自动生成上月房租账单

新增私有方法 `getResidentByName` / `removeResident`，改用 QueryWrapper 按 `owner_name` 查询

4. Controller 层— 合并不必要 Controller，统一返回类型为 `Result<T>`

新增的 REST 接口：

| Controller           | 请求方法 | 路径                      | 说明                           |
| -------------------- | -------- | ------------------------- | ------------------------------ |
| `HouseController`    | GET      | `/house/available`        | 查询所有空闲房屋（按房号升序） |
| `HouseController`    | GET      | `/house/info`             | 按房号查询房屋详情             |
| `HouseController`    | GET      | `/house/standard/list`    | 查询所有住房标准               |
| `HouseController`    | GET      | `/house/standard/info`    | 按面积查询住房标准             |
| `ResidentController` | GET      | `/resident/bill/list`     | 查询所有账单                   |
| `ResidentController` | POST     | `/resident/bill/generate` | 手动生成月度账单               |
| `AdminController`    | POST     | `/admin/updateStandard`   | 修改住房标准最低分数           |

`/housing-conditions`：原查询 `resident` 表（住户）→ 改为查询 `house_standard` 表（住房标准），返回面积与最低分数；新增按面积范围筛选参数

`/updateStandard`：新增： 修改指定面积的最低住房分数

并且新增了redis缓存，

退房删除住户改为按 `ownerName + houseNo`

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260605154731096.png)

增加了账单管理时的月份校验： 防止前端传空值或非法格式导致 SQL 异常。

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260605154758292.png)

进行前端代码编写并进行调试

首先建立测试表导入测试数据

```bash
mysql -u root -p123456 -e "CREATE DATABASE IF NOT EXISTS estate_management DEFAULT CHARACTER SET utf8mb4;"
mysql -u root -p123456 -D estate_management < 后端服务代码/Demo/src/main/resources/schema.sql
mysql -u root -p123456 -D estate_management < 后端服务代码/Demo/src/main/resources/data.sql
```

之后安装Redis for Windows 

从 NuGet 下载 Redis-64 包https://www.nuget.org/api/v2/package/Redis-64/3.0.503

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260605154820134.png)

测试输出: PONG

首先在无 Redis 模式启动

遇到的问题：

1. `RedissonAutoConfigurationV2` 不是 auto-configuration 类，排除失败
2. 需改为排除 `RedissonAutoConfigurationV2` 的正确类名
3. `DistributedLockUtil` 缺少 `RedissonClient` 时注入失败

解决方案：

- 修正 application.yml 排除类名
- `DistributedLockUtil` 增加本地锁回退（`@Autowired(required=false)` + `synchronized`）

接口测试结果：

查询类接口

| API                       | 状态码 | 说明                          |
| ------------------------- | ------ | ----------------------------- |
| `GET /application/list`   | 200    | 返回 9 条申请单（含测试数据） |
| `GET /application/queue`  | 200    | 返回 3 条排队分房申请         |
| `GET /house/list`         | 200    | 返回 10 套房屋                |
| `GET /house/available`    | 200    | 返回 5 套空闲房源             |
| `GET /resident/list`      | 200    | 返回 5 户住户                 |
| `GET /resident/bill/list` | 200    | 返回 10 条房租账单            |
| `GET /admin/statistics`   | 200    | 返回完整统计报表              |
| `GET /admin/threshold`    | 200    | 返回 5 条住房标准             |

写操作接口

| API                                      | 状态码 | 说明                     |
| ---------------------------------------- | ------ | ------------------------ |
| `POST /application/submit` (分房)        | 200    | 成功创建，评分 70        |
| `POST /application/submit` (分房-中文)   | 200    | 成功创建，评分 80        |
| `POST /application/submit` (退房-不匹配) | 500    | 正确拦截"未查到入住记录" |

前端代理

| 前端 URL                                     | 状态码 | 说明          |
| -------------------------------------------- | ------ | ------------- |
| `http://localhost:3000/api/application/list` | 200    | Vite 代理正常 |
| `http://localhost:3000/api/house/list`       | 200    | Vite 代理正常 |

