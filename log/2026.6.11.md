2026.6.1

目前存在这些问题;

问题1：调房申请缺少"原住房面积"字段

题目明确要求调房申请包含原住房面积，但前端 `ApplicationSubmit.vue:162` 提交调房申请时 `oldHouseArea` 写死为 `null`：

在调房申请表单中增加"原住房面积"输入框。

问题2：管理员"驳回"功能缺少后端接口，前端有按钮但后端无接口，点击会报 404

首先对问题一进行修改：

ApplicationSubmit.vue

新增"原住房面积"输入框

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260611155921541.png)

从 `null` 改为实际值

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260611155942545.png)

新增 oldHouseArea 必填校验

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260611160000501.png)

之后进行问题二的修改

添加驳回接口

ApplicationService.java：新增 `rejectApplication(applyNo, reason)` 方法

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260611160033235.png)

ApplicationController.java：新增 `POST /application/reject` 接口

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260611160056415.png)

检查申请单是否存在，检查是否待处理状态（已处理的不允许重复驳回），驳回原因不能为空，将 `applyStatus` 设为 2（已驳回），记录 `rejectReason` 和 `processTime`

之后加了Redis + 分布式锁

修改了 3 个文件

pom.xml：新增 `embedded-redis:1.4.3` 依赖

新建EmbeddedRedisConfig.java：应用启动时自动启动嵌入 Redis，停止时自动关闭

application.yml：`app.redis.enabled: true`，`cache.type: redis`

配置HTTPS 

新建了keystore.p12：自签名 SSL 证书，有效期 365 天

application.yml：端口 8080→8443，新增 ssl 配置块

vite.config.js：代理目标改为 `https://localhost:8443`，`secure: false`

application.yml 新增内容：

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260611160114911.png)

vite.config.js 代理修改：

```js
target: 'https://localhost:8443',  // HTTP → HTTPS
secure: false,                     // 自签名证书跳过验证
```

前端页面访问地址不变（`http://localhost:3000`），但前端向后端发起的 API 请求会走加密通道。

启动后端后，浏览器访问 `https://localhost:8443/admin/statistics`，忽略证书警告后应能看到 JSON 数据返回，说明 HTTPS 配置成功。

多线程并行分房 实现完成

新建了AsyncConfig.java：线程池 `estateExecutor`，核心 4 线程，最大 8 线程

ApplicationService.java：改造 `allocatePendingApplications()` 为多线程并行

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260611160133575.png)

敏感数据加解密 实现完成

新建了AesUtil.java：AES-128 加解密工具，基于 Hutool

ResidentService.java：全部读写方法增加加解密处理

ApplicationService.java:住户相关操作增加加解密处理

对 ownerName和department  VARCHAR进行AES 加密存储  

加密算法：AES-128 + Base64 编码              │
密钥：EstateMgmt@2026!   

全局 CSS 优化完成

新建了src/assets/global.css：集中定义 `.page-title` `.stat-card` `.stat-value` `.stat-label` `.quick-card` `.quick-name` `.section-card`

12 个 Vue 视图：删除各自 `<style scoped>` 中的重复 `.page-title`

Dashboard.vue：删除已移至全局的 `.stat-*` `.quick-*` `.section-card`

Statistics.vue：删除已移至全局的 `.stat-*`

提取 ResultDialog 公共组件

新增文件src/components/ResultDialog.vue：作为通用操作结果弹窗组件

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260611160156988.png)

对ApplicationService进行了拆分

新建了ScoreCalculator.java，作为住房分数计算工具类，StatisticsService.java进行报表的统计

增加了全局异常处理器，新建了GlobalExceptionHandler.java

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260611160217539.png)


