2026.6.12

现在是还有这几个问题

ScoreCalculator 分数计算 Bug — "副教授"被误判为"教授"

ScoreCalculator.java + StandardList.vue

```java
if (title.contains("教授") || title.contains("处长")) baseScore = 100;
```

`"副教授".contains("教授")` 返回 `true`，导致副教授得到 100 分（应为 80 分）。

需要在 ScoreCalculator.java 中把"副教授"提到前面：

![img](https://cdn.jsdelivr.net/gh/yuyuhan7634/yuyuhan7634-image-bed/image/20260612153620315.png)

问题 2：Statistics.vue — loading 变量定义了但没绑定到模板

`Statistics.vue`中 

```js
const loading = ref(false)  // 定义了
```

但模板中没有 `v-loading="loading"`，统计加载时页面无 loading 动画。

修改内容：  `<div class="statistics">` → `<div class="statistics" v-loading="loading">`

之后就是去写报告了